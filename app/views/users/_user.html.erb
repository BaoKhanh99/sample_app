<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if is_admin_delete? user %>
    |
    <%=
      link_to t(".btn_delete"), user, method: :delete, data: {confirm: t(".confirm_message")}
    %>
  <% end %>
</li>
